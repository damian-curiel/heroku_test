/* tslint:disable:all */
/**
 * Interface Payouts cash Service file.
 */
import { Observable } from "rxjs";
import { PayoutsCashTokenRequest } from "./../../lib/types/payouts_cash_request";
import { TokenResponse } from "./../../lib/types/remote/token_response";
export interface IPayoutsCashService {
    /**
     *  Payouts cash token
     * @param body - payout cash token request body
     * @param mid - public merchant id
     * @param isTest - is test environment
     * @param regional - Define if endpoint used regional URL
     */
    requestPayoutsCashToken(body: PayoutsCashTokenRequest, mid: string, isTest: boolean, regional: boolean): Observable<TokenResponse>;
}
