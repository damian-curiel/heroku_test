/* tslint:disable:all */
/**
 * Interface Payouts transfer Service file.
 */
import { Observable } from "rxjs";
import { BankListResponse } from "./../../lib/types/bank_list_response";
import { PayoutsTransferTokenRequest } from "./../../lib/types/payouts_transfer_token_request";
import { TokenResponse } from "./../../lib/types/remote/token_response";
export interface IPayoutsTransferService {
    /**
     *  Payouts cash token
     * @param body - payout transfer token request body
     * @param mid - public merchant id
     * @param isTest - is test environment
     * @param regional - Define if endpoint used regional URL
     */
    requestPayoutsTransferToken(body: PayoutsTransferTokenRequest, mid: string, isTest: boolean, regional: boolean): Observable<TokenResponse>;
    /**
     *  Payouts transfer banklist
     * @param mid - public merchant id
     * @param isTest - is test environment
     * @param regional - Define if endpoint used regional URL
     */
    requestPayoutsTransferBankList(mid: string, isTest: boolean, regional: boolean): Observable<BankListResponse>;
}
