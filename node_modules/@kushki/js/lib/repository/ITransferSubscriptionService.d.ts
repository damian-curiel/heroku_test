/* tslint:disable:all */
import { Observable } from "rxjs";
import { BankListResponse } from "./../../lib/types/bank_list_response";
import { TokenResponse } from "./../../lib/types/remote/token_response";
import { TransferSubscriptionTokenRequest } from "./../../lib/types/transfer_subscription_token_request";
export interface ITransferSubscriptionService {
    /**
     *  Transfer subscription Token
     * @param tokenRequest - transfer subscriptions tokens request body
     * @param mid - public merchant id
     * @param isTest - is test environment
     * @param regional - Define if endpoint used regional URL
     */
    requestToken(tokenRequest: TransferSubscriptionTokenRequest, mid: string, isTest: boolean, regional: boolean): Observable<TokenResponse>;
    /**
     *  Bank List
     * @param mid - public merchant id
     * @param isTest - is test environment
     * @param regional - Define if endpoint used regional URL
     */
    requestBankList(mid: string, isTest: boolean, regional: boolean): Observable<BankListResponse>;
}
